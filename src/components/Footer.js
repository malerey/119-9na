import Button from "./Button"

const Footer = ({  }) => {
  return (
    <footer>
      <h2>Soy footer</h2>
      <input type="email" />
      <Button title="Suscribirse al newsletter" />
    </footer>
  )
}

export default Footer
